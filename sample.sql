select
        data_date,  -- 日期(string)
        dp,   -- 端(string)
        user_type,  -- 用户类型(string)
        web_channel, --用户类型(string)
        web_sub_channel, --一级渠道(string)
        pv, --二级渠道(bigint)
        uv, -- pv (bigint)
        visits, --uv (bigint)
        bounce_num, --跳出数 (bigint)
        bounce_rate, --跳出率 (double)
        sj, --商机量 (bigint)
        sj_uv, -- 商机用户 (bigint)
        tel,  -- 电话商机 (bigint)
        im, -- im商机(bigint)
        liuzi -- 留资商机(bigint)
from








